node_modules/
# Synced build artifacts (copied from the backend/frontend repos by `npm run sync`)
backend/
frontend/
# electron-builder output
release/
.DS_Store

# JetBrains IDE state — local to whoever opened the project.
.idea/
